endpoint: "http://localhost:8000"
tests:
  - path: "/api/user"
    method: "POST"
    name: "Create user"
    form:
      type: urlencoded
      values:
        name:
          - hola
        password:
          - hola
        email:
          - hola+1@hola.com
    expected:
        statusCode: 201
        body-type: string
        body: ""
